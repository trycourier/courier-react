{
  "name": "@trycourier/react-footer",
  "version": "1.36.0",
  "description": "",
  "main": "dist/index.js",
  "types": "typings/index.d.ts",
  "scripts": {
    "test": "jest -c jest.config.js --runInBand",
    "babel": "babel src -d dist --extensions \".ts,.tsx\" --ignore \"src/**/__tests__/**\"",
    "build:watch": "yarn babel --watch",
    "build": "rimraf dist && yarn babel",
    "clean": "rimraf dist",
    "type-check": "tsc --noEmit",
    "readme": "concat-md --toc --decrease-title-levels --dir-name-as-title docs > README.md",
    "types": "tsc --build"
  },
  "license": "ISC",
  "peerDependencies": {
    "react": "^17.0.1"
  },
  "files": [
    "dist/",
    "typings/"
  ],
  "exports": {
    ".": "./dist/index.js",
    "./use-inbox": "./dist/inbox/index.js"
  }
}
